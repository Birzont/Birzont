"use client";

type BlobBackgroundProps = {
  variant?: "dark" | "light" | "cta";
  className?: string;
};

/** Flat theme background only — decorative blobs removed. */
export function BlobBackground(_props: BlobBackgroundProps) {
  return null;
}
